<form method="post" action="{{ path('app_sprite_delete', {'id': sprite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sprite.id) }}">
    <button class="btn">Delete</button>
</form>
