{% extends 'base.html.twig' %}

{% block title %}Edit Saves{% endblock %}

{% block body %}
    <h1>Edit Saves</h1>

    {{ include('saves/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_saves_index') }}">back to list</a>

    {{ include('saves/_delete_form.html.twig') }}
{% endblock %}
