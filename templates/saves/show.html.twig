{% extends 'base.html.twig' %}

{% block title %}Saves{% endblock %}

{% block body %}
    <h1>Saves</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ save.id }}</td>
            </tr>
            <tr>
                <th>Pos_x</th>
                <td>{{ save.posX }}</td>
            </tr>
            <tr>
                <th>Pos_y</th>
                <td>{{ save.posY }}</td>
            </tr>
            <tr>
                <th>VidasRestantes</th>
                <td>{{ save.vidasRestantes }}</td>
            </tr>
            <tr>
                <th>Fecha_guardado</th>
                <td>{{ save.fechaGuardado ? save.fechaGuardado|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_saves_index') }}">back to list</a>

    <a href="{{ path('app_saves_edit', {'id': save.id}) }}">edit</a>

    {{ include('saves/_delete_form.html.twig') }}
{% endblock %}
