{% extends 'base.html.twig' %}

{% block title %}UserPlayer{% endblock %}

{% block body %}
    <h1>UserPlayer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_player.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_player_index') }}">back to list</a>

    <a href="{{ path('app_user_player_edit', {'id': user_player.id}) }}">edit</a>

    {{ include('user_player/_delete_form.html.twig') }}
{% endblock %}
